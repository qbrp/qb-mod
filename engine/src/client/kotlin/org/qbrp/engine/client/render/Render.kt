package org.qbrp.engine.client.render

import org.qbrp.engine.client.render.hud.HudInitializer

object Render {
    //val hudInitializer = HudInitializer()

    fun initialize() {
        //PlayerIconRenderer().initialize()
        //hudRender.initialize()
    }
}